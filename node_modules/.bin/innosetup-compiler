#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@quanle94/innosetup/lib/iscc" "$@"
  ret=$?
else 
  node  "$basedir/../@quanle94/innosetup/lib/iscc" "$@"
  ret=$?
fi
exit $ret
