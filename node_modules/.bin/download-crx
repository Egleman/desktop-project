#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../download-crx/bin/download" "$@"
  ret=$?
else 
  node  "$basedir/../download-crx/bin/download" "$@"
  ret=$?
fi
exit $ret
